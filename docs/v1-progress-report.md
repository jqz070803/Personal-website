# 个人主页 · 迭代进度报告

> 作者：江沁钊（智能医学工程 · 大一）
> 课程：Vibe Coding（个人网页主页项目）
> 本项目文档用于**可追溯地记录每一次迭代的进步**，并在最后以 PDF 形式输出。
> 每个版本均附截图（见 `artifacts/screenshots/`）。

---

## 版本时间线

| 版本 | 日期 | 阶段 | 关键产出 | 截图 |
| :---: | :---: | :---: | --- | --- |
| **v1** | 2026-09-03 | MVP 最小可行产品 | 完整个人主页（响应式 + 智能体预留位 + 多功能块）；视觉升级：星空山峦背景 + 关于我左图右文 + 整体深色主题 + 滚动背景渐变与滑动入场 + 社交区精简与真实信息接入 + "我的技能"区块 + 技能卡 emoji + 标题"笔尖书写"动效 | `v1-skills.png` / `v1-skills-mobile.png` |
| v2 | 待开发 | Git / checkpoint 存档 + 功能完善 | - | - |
| v3 | 待开发 | Supabase Dashboard + Feedback | - | - |
| v4 | 待开发 | 数字孪生 / AI 集成 | - | - |

---

## v1 · 本次迭代（MVP）

### 🎯 版本目标
- 完成一个**大气、简约、以"山川湖海"为主题**的个人网页主页。
- 适用于三类场景：简历/面试给 HR、陌生初次见面介绍自己、老师/朋友浏览。
- 桌面端与手机端均能正确显示（响应式）。
- 为后续迭代预留结构：**智能体（Agent）位置**。

### 🎨 视觉设计感升级（第 2 轮）
- **首屏背景**：由纯色/单一渐变，加入**星空微光 + 层叠山峦剪影**矢量背景，
  贴合"山川湖海"主题，打破纯色、更有场景层次感；文字保持聚焦，不影响可读性。
- **"关于我"区块重构**（下滑第一个区块）：改为 **左个人照片 / 右个人信息 + 科研获奖成果** 左右布局。
  - 左：照片帧（占位，待替换真实照片），含说明"江沁钊 · 行走于山川湖海"。
  - 右：`一处坐标`、`一个灵魂` 信息卡 + `科研 · 获奖成果` 卡（目前为空位占位，待逐步补充）。
  - 手机端自动转为上下堆叠，照片居中。
- 桌面端 / 移动端截图均已重新验证通过。

### 🌌 整体深色主题 + 滚动动效升级（第 3 轮）
- **整站改深色夜景基调**：去除"深色 hero → 突然跳浅色"的生硬切换，全页统一为深空蓝黑基调，
  文字、卡片、导航、分区全部适配，背景连贯流畅（`--paper` / `--card` 等变量重写为深色）。
- **滚动背景色渐变**：随鼠标下滑，页面背景在 5 档深空色带之间缓慢、平滑地流动
  （含非线性缓动），背景颜色不会生硬跳变，贴合"穿越山海"的沉浸感。
- **模块滑动入场动画**：各区块/卡片支持上浮、左右滑入与**交错延迟**入场
  （`data-dir` / `data-stagger`），下滑时内容依次错落浮现；包含 `.reveal` 增强。
- 移动端汉堡菜单、分隔线等一并适配深色主题。
- 桌面端 / 移动端截图均已重新验证通过。

### 🔊 社交区精简 + 真实信息接入（第 4 轮）
- **"在别处也能遇到我"改为三个紧凑小模块**：去掉了三张重复"不会飞的jiang"的大卡片，
  改成带**平台图标 + 平台名 + 账号 + 箭头**的精炼小卡，内容不多不再占据大片版面。
- **哔哩哔哩**：接入真实个人主页链接 `space.bilibili.com/1368246027`。
- **抖音**：展示真实抖音号 `dyj79uff0pb4`。
- **视频号**：展示账号「不会飞的jiang」。
- **联系区**：写入真实邮箱「联系我：jqz0803@qq.com」（mailto 可直接点击写信）。
- 访问逻辑更新：B站为真实链接直接跳转；抖音 / 视频号为占位，点击给出占位提示。
- 桌面端 / 移动端截图均已重新验证通过。

### 🎲 "我的技能"区块新增（第 5 轮）
- **新增"我的技能"区块**（板块编号 04，位于"镜头"之后）：8 张趣味技能卡
  （偷睡漏睡达人 / 麻将初学者 / 雪山冲浪气氛组 / 灵魂画手 / 高度上瘾飞行家 /
  新手村永久荣誉村民 / 山川湖海人形记录仪 / 非典型内卷幸存者），
  每张卡由**线性 SVG 图标 + 技能名 + 一句调侃描述**组成，呼应"不按理出牌"的气质。
- **导航新增"技能"锚点**，与区块联动（点击跳转到"我的技能"）。
- **板块编号重排**：镜头=03、技能=04、社交=05、智能体=06、联系=07，保持一致连贯。
- 移动端技能墙自动排为 **4×2 双列**，卡片文字换行正常、无溢出；桌面端 4 列。
- 卡片采用 `minmax(0,1fr)` 网格轨道 + `overflow-wrap:anywhere`，杜绝内容挤压溢出。
- 桌面端 / 移动端截图均已重新验证通过。

### ✍️ 技能卡加 emoji + "笔尖书写"动效（第 7 轮）
- **技能卡每一段文字都配上贴切的 emoji**：8 张技能卡的技能名与调侃描述句尾各加了一个
  与主题呼应的 emoji（😴 🀄 🏂 🎨 ✈️ 🏡 📷 🌿 / 💤 🎲 ⛷️ 🖌️ 🛫 🌾 🗂️ 🛟），
  让"不按理出牌"的个性标签更具画面感。
- **"笔尖书写"动效（类似 Windows 激活屏 hello 的丝滑一笔一画写出）**：给 hero 大标题与各区块标题
  加上 `[data-write]` 标记，用一道柔和的"墨迹"前沿从左向右扫过，文字像被笔尖书写出来——
  - hero 大标题在页面加载时按时间轴书写（约 1.7s，ease-out）；
  - 其余区块标题在滚动进入视口时按滚动进度书写（进入视口底部开始写，滚到视口上方 30% 处写完）；
  - 技术：CSS `mask-image` 线性渐变 + `--write-progress` 变量控制扫到哪，无需拆分 DOM，中文/响应式
    均安全（此前"逐字上浮渐显"方案已按反馈废弃，改为真正的笔迹描边滑过）；
  - 截图模式（`?shot=1`）与 `prefers-reduced-motion` 下直接整段显示，不干扰截图与无障碍。
- 已用 `?shot=1` 全页截图 + hero 实时截图验证：书写动效丝滑正常、8 张卡 emoji 齐全、桌面 4×2 网格无溢出。

### ✨ 本次完成的功能块
1. **首屏 Hero**：大气的山川湖海渐变背景（暂以渐变模拟，待上传真实照片替换），
   全名 + 一句自我介绍 + 身份标签（旅行摄影 / 航拍 / 剪辑 / 乐于助人）+ 行动按钮。
2. **关于我**：包含"一处坐标"（成都 · 智能医学工程）与"一个灵魂"（现代徐霞客 · 时 i 时 e · 乐于助人）两个卡片。
3. **数据统计**：足迹城市 / 定格的瞬间 / 山与湖 / 热爱（带数字滚动动效）。
4. **山川湖海足迹**：山、湖、海、城与人 四个旅行主题卡片（占位图，待替换照片）。
5. **镜头里的世界**：摄影、航拍、剪辑三个能力卡片（改用线性 SVG 图标）。
6. **我的技能**：8 张趣味技能卡（线性图标 + 技能名 + 调侃描述），凸显个性。
7. **社交媒体入口**：抖音 / 哔哩哔哩 / 视频号 同名账号「不会飞的jiang」入口卡片（已接入真实信息）。
8. **智能体预留区 ⭐**：预留的 AI 助手位，标注"尚在孵化中 · V2+ 接入"，为后续课程接入智能体留出位置。
9. **联系我**：邮件入口 + 提示。

### 🔄 本轮视觉升级（Apple 官网风）
根据反馈：**去掉左上角品牌名、去除"土气感"，转向 Apple 官网式极简高级**。
- **导航**：改为居中对齐、透明背景（滚动后毛玻璃），移除左上角名字与 CTA 按钮。
- **配色**：去掉花哨的金色渐变，改用冷峻深邃的深空蓝 + 白色 + Apple 系蓝（`#0a84ff`）的点缀，
  页面底色为大面积白色留白 + 浅灰（`#f5f5f7`）分区。
- **字体**：统一现代无衬线（SF Pro / PingFang），标题大号、克制字重、收紧字距（letter-spacing -0.02em）。
- **图标**：镜头区改用简洁的线性 SVG 图标（相机 / 无人机 / 播放器），去除 emoji。
- **光影**：卡片使用轻阴影 + 更柔和的 hover，hero 夜景加深渐晕让文字更聚焦。
- 桌面端与移动端截图均已重新验证通过。

### 📱 技术要点
- **纯前端静态实现**：`HTML + CSS + JS`，无外部框架依赖，无需构建即可打开，稳定且易于后期扩展。
- **响应式设计**：桌面（≥760px 多列网格）与手机（<760px 单列堆叠 + 汉堡菜单）自适应。
- **滚动显现动效**、**数字滚动统计**、**导航滚动变色**、**移动端汉堡菜单**等交互动效。
- **截图/预览模式**：URL 加 `?shot=1` 可即时显示全部区块，便于截图与打印。

### 🔧 文件结构（v1）
```
v1-web/
├── v1-index.html      # 主页结构
├── v1-style.css       # 样式（主题 / 响应式）
├── v1-script.js       # 交互逻辑
└── assets/            # (预留) 照片 / 资源
docs/
└── v1-progress-report.md   # 本进度报告
artifacts/
└── screenshots/            # 各版本截图
```

### 🚧 待办与后续方向（已记录，供 v2 继续）
- [ ] **上传真实照片**：替换首屏背景与足迹卡片占位图（我稍后会提示用户上传拍摄的山川湖海照片）。
- [ ] 接入真实社交媒体主页链接（抖音 / B站 / 视频号）。
- [ ] **智能体接入**：在预留区接入 AI 助手（V2+）。
- [ ] 建立 Git 版本控制 checkpoint（"后悔药"机制，已初始化仓库）。
- [ ] V2 起：接入 Supabase Dashboard + Feedback 等后端能力。

### 🏷️ 本次迭代自评
- 成功搭建了完整、美观、可用的个人主页 MVP。
- 响应式布局在桌面 / 移动端截图均验证通过。
- 智能体位置、后续扩展结构已提前预留，为 v2-v4 的演进打下基础。
